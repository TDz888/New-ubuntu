name: 🚀 VPS CREATOR PRO

on:
  workflow_dispatch:
    inputs:
      os_type:
        description: 🖥️ Operating System
        required: true
        default: windows11
        type: choice
        options:
          - windows11
          - windows10
          - ubuntu-server
      username:
        description: 👤 Username
        required: true
        default: vpsuser
      password:
        description: 🔑 Password
        required: true
        default: Vps123456
      region:
        description: 🌍 Region
        required: true
        default: us
        type: choice
        options:
          - us
          - eu
          - asia
      instances:
        description: 🔢 Number of VPS (1-3)
        required: true
        default: '1'
        type: choice
        options:
          - '1'
          - '2'
          - '3'
      wallpaper_url:
        description: 🖼️ Wallpaper URL
        required: false
        default: ''
      duration:
        description: ⏱️ Hours
        required: true
        default: '6'
        type: choice
        options:
          - '1'
          - '2'
          - '4'
          - '6'

jobs:
  create-vps-1:
    runs-on: windows-latest
    if: github.event.inputs.instances >= '1'
    timeout-minutes: 360
    
    steps:
      - name: Init
        shell: pwsh
        run: |
          $os = "${{ github.event.inputs.os_type }}"
          if ($os -eq "windows11") { $icon = "🪟✨"; $name = "Windows 11 Pro" }
          elseif ($os -eq "windows10") { $icon = "🪟"; $name = "Windows 10 Pro" }
          else { $icon = "🐧⚡"; $name = "Ubuntu Server" }
          Write-Host ""
          Write-Host "╔════════════════════════════════════════════════════╗"
          Write-Host "║  $icon $name - INSTANCE 1 $icon                ║"
          Write-Host "╚════════════════════════════════════════════════════╝"
          Write-Host "  🌍 Region : ${{ github.event.inputs.region }}"
          Write-Host "  👤 User   : ${{ github.event.inputs.username }}"
          Write-Host "  ⏱️ Hours  : ${{ github.event.inputs.duration }}"
          Write-Host ""
          
      - name: RDP
        shell: pwsh
        run: |
          Write-Host "> Configuring RDP..."
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          netsh advfirewall firewall add rule name="RDP-In" dir=in action=allow protocol=TCP localport=3389
          Set-Service -Name TermService -StartupType Automatic
          Start-Service -Name TermService
          Write-Host "  ✅ RDP ready"
          
      - name: User
        shell: pwsh
        run: |
          Write-Host "> Creating user..."
          $u = "${{ github.event.inputs.username }}"
          $p = "${{ github.event.inputs.password }}"
          $sec = ConvertTo-SecureString $p -AsPlainText -Force
          try { Remove-LocalUser -Name $u -ErrorAction SilentlyContinue } catch {}
          New-LocalUser -Name $u -Password $sec -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member $u
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member $u
          Enable-LocalUser -Name $u
          $p | Out-File -FilePath $env:TEMP\pass.txt
          Write-Host "  ✅ User: $u"
          
      - name: Wallpaper
        if: github.event.inputs.wallpaper_url != ''
        shell: pwsh
        run: |
          Write-Host "> Setting wallpaper..."
          $url = "${{ github.event.inputs.wallpaper_url }}"
          $img = "$env:TEMP\wall.jpg"
          Invoke-WebRequest -Uri $url -OutFile $img
          Set-ItemProperty -Path "HKCU:\Control Panel\Desktop" -Name Wallpaper -Value $img
          Set-ItemProperty -Path "HKCU:\Control Panel\Desktop" -Name WallpaperStyle -Value 2
          Write-Host "  ✅ Wallpaper applied"
          
      - name: Screenshot
        shell: pwsh
        run: |
          Write-Host "> Capturing screenshot..."
          Add-Type -AssemblyName System.Windows.Forms,System.Drawing
          $bmp = New-Object System.Drawing.Bitmap ([System.Windows.Forms.Screen]::PrimaryScreen.Bounds.Width), ([System.Windows.Forms.Screen]::PrimaryScreen.Bounds.Height)
          $g = [System.Drawing.Graphics]::FromImage($bmp)
          $g.CopyFromScreen(0, 0, 0, 0, $bmp.Size)
          $bmp.Save("$env:TEMP\screen.png")
          $g.Dispose(); $bmp.Dispose()
          Write-Host "  ✅ Screenshot saved"
          
      - name: Tailscale
        shell: pwsh
        env:
          KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
        run: |
          Write-Host "> Installing Tailscale..."
          $msi = "$env:TEMP\ts.msi"
          Invoke-WebRequest -Uri "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi" -OutFile $msi
          Start-Process msiexec.exe -ArgumentList "/i", "`"$msi`"", "/quiet", "/norestart" -Wait
          Remove-Item $msi -Force
          Write-Host "  ✅ Tailscale installed"
          
      - name: Connect
        shell: pwsh
        env:
          KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
        run: |
          Write-Host "> Connecting to Tailscale..."
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=$env:KEY --hostname="vps-1"
          Start-Sleep -Seconds 5
          $ip = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
          echo "IP=$ip" >> $env:GITHUB_ENV
          Write-Host "  ✅ Connected - IP: $ip"
          
      - name: Benchmark
        shell: pwsh
        run: |
          Write-Host "> Running benchmark..."
          $c = Get-Date
          $s = 0
          for ($i=0; $i -lt 1000000; $i++) { $s += $i }
          $cpu = [math]::Round(1000000 / (Get-Date).Subtract($c).TotalMilliseconds * 100, 2)
          
          $m = Get-Date
          $arr = New-Object System.Collections.ArrayList
          for ($i=0; $i -lt 500000; $i++) { $arr.Add($i) | Out-Null }
          $mem = [math]::Round(500000 / (Get-Date).Subtract($m).TotalMilliseconds * 100, 2)
          
          $d = Get-Date
          "X" * 10485760 | Out-File "$env:TEMP\disk.tmp"
          $disk = [math]::Round(10 / (Get-Date).Subtract($d).TotalSeconds, 2)
          Remove-Item "$env:TEMP\disk.tmp" -Force
          
          $bench = @()
          $bench += "CPU: $cpu ops/sec"
          $bench += "Memory: $mem ops/sec"
          $bench += "Disk: $disk MB/s"
          $bench | Out-File "$env:TEMP\bench.txt"
          Write-Host "  ✅ Benchmark done"
          
      - name: Monitor
        shell: pwsh
        run: |
          Write-Host "> Collecting resource data..."
          $cpu = (Get-WmiObject Win32_Processor).LoadPercentage
          $ram = Get-WmiObject Win32_OperatingSystem
          $ramU = [math]::Round(($ram.TotalVisibleMemorySize - $ram.FreePhysicalMemory) / 1MB, 2)
          $ramT = [math]::Round($ram.TotalVisibleMemorySize / 1MB, 2)
          $disk = Get-WmiObject Win32_LogicalDisk -Filter "DeviceID='C:'"
          $diskU = [math]::Round(($disk.Size - $disk.FreeSpace) / 1GB, 2)
          $diskT = [math]::Round($disk.Size / 1GB, 2)
          $up = (Get-Date) - (Get-CimInstance Win32_OperatingSystem).LastBootUpTime
          
          $res = @()
          $res += "CPU: $cpu%"
          $res += "RAM: $ramU/$ramT GB"
          $res += "DISK: $diskU/$diskT GB"
          $res += "UPTIME: $($up.Days)d $($up.Hours)h"
          $res | Out-File "$env:TEMP\res.txt"
          
          $hist = "$env:TEMP\history.csv"
          if (-not (Test-Path $hist)) { "Time,CPU,RAM_GB,DISK_GB" | Out-File $hist }
          "$(Get-Date -Format 'HH:mm:ss'),$cpu,$ramU,$diskU" | Out-File $hist -Append
          Write-Host "  ✅ Data collected"
          
      - name: Info
        shell: pwsh
        run: |
          $pass = Get-Content "$env:TEMP\pass.txt"
          $bench = Get-Content "$env:TEMP\bench.txt"
          $res = Get-Content "$env:TEMP\res.txt"
          $ip = "$env:IP"
          $user = "${{ github.event.inputs.username }}"
          $hours = "${{ github.event.inputs.duration }}"
          $start = (Get-Date).ToString('HH:mm:ss')
          $end = (Get-Date).AddHours($hours).ToString('HH:mm:ss')
          
          Write-Host ""
          Write-Host "╔════════════════════════════════════════════════════╗"
          Write-Host "║  🎉 VPS READY - CONNECT NOW 🎉                     ║"
          Write-Host "╠════════════════════════════════════════════════════╣"
          Write-Host "║  🌐 IP      : $ip"
          Write-Host "║  👤 User    : $user"
          Write-Host "║  🔑 Pass    : $pass"
          Write-Host "║  🔌 Port    : 3389"
          Write-Host "╠════════════════════════════════════════════════════╣"
          Write-Host "║  📊 BENCHMARK:"
          foreach ($l in $bench) { Write-Host "║     $l" }
          Write-Host "╠════════════════════════════════════════════════════╣"
          Write-Host "║  📊 RESOURCES:"
          foreach ($l in $res) { Write-Host "║     $l" }
          Write-Host "╠════════════════════════════════════════════════════╣"
          Write-Host "║  🕐 Started : $start"
          Write-Host "║  🕐 Expires : $end"
          Write-Host "╚════════════════════════════════════════════════════╝"
          Write-Host ""
          
          $info = @()
          $info += "╔════════════════════════════════════════════════════╗"
          $info += "║  VPS CONNECTION INFO                                 ║"
          $info += "╠════════════════════════════════════════════════════╣"
          $info += "║  Host : $ip"
          $info += "║  Port : 3389"
          $info += "║  User : $user"
          $info += "║  Pass : $pass"
          $info += "╠════════════════════════════════════════════════════╣"
          $info += "║  BENCHMARK:"
          foreach ($l in $bench) { $info += "║    $l" }
          $info += "╠════════════════════════════════════════════════════╣"
          $info += "║  RESOURCES:"
          foreach ($l in $res) { $info += "║    $l" }
          $info += "╠════════════════════════════════════════════════════╣"
          $info += "║  Started : $start"
          $info += "║  Expires : $end"
          $info += "╚════════════════════════════════════════════════════╝"
          $info | Out-File vps-info.txt -Encoding ascii
          
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: vps-data-1
          path: |
            vps-info.txt
            ${{ env.TEMP }}\screen.png
            ${{ env.TEMP }}\bench.txt
            ${{ env.TEMP }}\history.csv
          retention-days: 7
          
      - name: Keep
        shell: pwsh
        run: |
          $h = ${{ github.event.inputs.duration }}
          Write-Host "🟢 VPS running for $h hours"
          Start-Sleep -Seconds ($h * 3600)

  create-vps-2:
    runs-on: windows-latest
    if: github.event.inputs.instances >= '2'
    timeout-minutes: 360
    steps:
      - name: Init
        shell: pwsh
        run: |
          $os = "${{ github.event.inputs.os_type }}"
          if ($os -eq "windows11") { $icon = "🪟✨"; $name = "Windows 11 Pro" }
          elseif ($os -eq "windows10") { $icon = "🪟"; $name = "Windows 10 Pro" }
          else { $icon = "🐧⚡"; $name = "Ubuntu Server" }
          Write-Host ""
          Write-Host "╔════════════════════════════════════════════════════╗"
          Write-Host "║  $icon $name - INSTANCE 2 $icon                ║"
          Write-Host "╚════════════════════════════════════════════════════╝"
          Write-Host "  🌍 Region : ${{ github.event.inputs.region }}"
          Write-Host "  👤 User   : ${{ github.event.inputs.username }}"
          Write-Host "  ⏱️ Hours  : ${{ github.event.inputs.duration }}"
          
      - name: RDP
        shell: pwsh
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          netsh advfirewall firewall add rule name="RDP-In" dir=in action=allow protocol=TCP localport=3389
          Set-Service -Name TermService -StartupType Automatic
          Start-Service -Name TermService
          
      - name: User
        shell: pwsh
        run: |
          $u = "${{ github.event.inputs.username }}"
          $p = "${{ github.event.inputs.password }}"
          $sec = ConvertTo-SecureString $p -AsPlainText -Force
          try { Remove-LocalUser -Name $u -ErrorAction SilentlyContinue } catch {}
          New-LocalUser -Name $u -Password $sec -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member $u
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member $u
          Enable-LocalUser -Name $u
          $p | Out-File -FilePath $env:TEMP\pass.txt
          
      - name: Wallpaper
        if: github.event.inputs.wallpaper_url != ''
        shell: pwsh
        run: |
          $url = "${{ github.event.inputs.wallpaper_url }}"
          $img = "$env:TEMP\wall.jpg"
          Invoke-WebRequest -Uri $url -OutFile $img
          Set-ItemProperty -Path "HKCU:\Control Panel\Desktop" -Name Wallpaper -Value $img
          Set-ItemProperty -Path "HKCU:\Control Panel\Desktop" -Name WallpaperStyle -Value 2
          
      - name: Screenshot
        shell: pwsh
        run: |
          Add-Type -AssemblyName System.Windows.Forms,System.Drawing
          $bmp = New-Object System.Drawing.Bitmap ([System.Windows.Forms.Screen]::PrimaryScreen.Bounds.Width), ([System.Windows.Forms.Screen]::PrimaryScreen.Bounds.Height)
          $g = [System.Drawing.Graphics]::FromImage($bmp)
          $g.CopyFromScreen(0, 0, 0, 0, $bmp.Size)
          $bmp.Save("$env:TEMP\screen.png")
          $g.Dispose(); $bmp.Dispose()
          
      - name: Tailscale
        shell: pwsh
        env:
          KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
        run: |
          $msi = "$env:TEMP\ts.msi"
          Invoke-WebRequest -Uri "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi" -OutFile $msi
          Start-Process msiexec.exe -ArgumentList "/i", "`"$msi`"", "/quiet", "/norestart" -Wait
          Remove-Item $msi -Force
          
      - name: Connect
        shell: pwsh
        env:
          KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=$env:KEY --hostname="vps-2"
          Start-Sleep -Seconds 5
          $ip = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
          echo "IP=$ip" >> $env:GITHUB_ENV
          
      - name: Benchmark
        shell: pwsh
        run: |
          $c = Get-Date
          $s = 0
          for ($i=0; $i -lt 1000000; $i++) { $s += $i }
          $cpu = [math]::Round(1000000 / (Get-Date).Subtract($c).TotalMilliseconds * 100, 2)
          $m = Get-Date
          $arr = New-Object System.Collections.ArrayList
          for ($i=0; $i -lt 500000; $i++) { $arr.Add($i) | Out-Null }
          $mem = [math]::Round(500000 / (Get-Date).Subtract($m).TotalMilliseconds * 100, 2)
          $d = Get-Date
          "X" * 10485760 | Out-File "$env:TEMP\disk.tmp"
          $disk = [math]::Round(10 / (Get-Date).Subtract($d).TotalSeconds, 2)
          Remove-Item "$env:TEMP\disk.tmp" -Force
          $bench = @()
          $bench += "CPU: $cpu ops/sec"
          $bench += "Memory: $mem ops/sec"
          $bench += "Disk: $disk MB/s"
          $bench | Out-File "$env:TEMP\bench.txt"
          
      - name: Monitor
        shell: pwsh
        run: |
          $cpu = (Get-WmiObject Win32_Processor).LoadPercentage
          $ram = Get-WmiObject Win32_OperatingSystem
          $ramU = [math]::Round(($ram.TotalVisibleMemorySize - $ram.FreePhysicalMemory) / 1MB, 2)
          $ramT = [math]::Round($ram.TotalVisibleMemorySize / 1MB, 2)
          $disk = Get-WmiObject Win32_LogicalDisk -Filter "DeviceID='C:'"
          $diskU = [math]::Round(($disk.Size - $disk.FreeSpace) / 1GB, 2)
          $diskT = [math]::Round($disk.Size / 1GB, 2)
          $up = (Get-Date) - (Get-CimInstance Win32_OperatingSystem).LastBootUpTime
          $res = @()
          $res += "CPU: $cpu%"
          $res += "RAM: $ramU/$ramT GB"
          $res += "DISK: $diskU/$diskT GB"
          $res += "UPTIME: $($up.Days)d $($up.Hours)h"
          $res | Out-File "$env:TEMP\res.txt"
          $hist = "$env:TEMP\history.csv"
          if (-not (Test-Path $hist)) { "Time,CPU,RAM_GB,DISK_GB" | Out-File $hist }
          "$(Get-Date -Format 'HH:mm:ss'),$cpu,$ramU,$diskU" | Out-File $hist -Append
          
      - name: Info
        shell: pwsh
        run: |
          $pass = Get-Content "$env:TEMP\pass.txt"
          $bench = Get-Content "$env:TEMP\bench.txt"
          $res = Get-Content "$env:TEMP\res.txt"
          $ip = "$env:IP"
          $user = "${{ github.event.inputs.username }}"
          $hours = "${{ github.event.inputs.duration }}"
          $start = (Get-Date).ToString('HH:mm:ss')
          $end = (Get-Date).AddHours($hours).ToString('HH:mm:ss')
          Write-Host ""
          Write-Host "╔════════════════════════════════════════════════════╗"
          Write-Host "║  🎉 VPS READY - INSTANCE 2 🎉                      ║"
          Write-Host "╠════════════════════════════════════════════════════╣"
          Write-Host "║  🌐 IP      : $ip"
          Write-Host "║  👤 User    : $user"
          Write-Host "║  🔑 Pass    : $pass"
          Write-Host "║  🔌 Port    : 3389"
          Write-Host "╠════════════════════════════════════════════════════╣"
          Write-Host "║  📊 BENCHMARK:"
          foreach ($l in $bench) { Write-Host "║     $l" }
          Write-Host "╠════════════════════════════════════════════════════╣"
          Write-Host "║  📊 RESOURCES:"
          foreach ($l in $res) { Write-Host "║     $l" }
          Write-Host "╠════════════════════════════════════════════════════╣"
          Write-Host "║  🕐 Started : $start"
          Write-Host "║  🕐 Expires : $end"
          Write-Host "╚════════════════════════════════════════════════════╝"
          Write-Host ""
          $info = @()
          $info += "Host: $ip"
          $info += "Port: 3389"
          $info += "User: $user"
          $info += "Pass: $pass"
          $info | Out-File vps-info.txt -Encoding ascii
          
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: vps-data-2
          path: |
            vps-info.txt
            ${{ env.TEMP }}\screen.png
            ${{ env.TEMP }}\bench.txt
            ${{ env.TEMP }}\history.csv
          retention-days: 7
          
      - name: Keep
        shell: pwsh
        run: |
          $h = ${{ github.event.inputs.duration }}
          Start-Sleep -Seconds ($h * 3600)

  create-vps-3:
    runs-on: windows-latest
    if: github.event.inputs.instances >= '3'
    timeout-minutes: 360
    steps:
      - name: Init
        shell: pwsh
        run: |
          $os = "${{ github.event.inputs.os_type }}"
          if ($os -eq "windows11") { $icon = "🪟✨"; $name = "Windows 11 Pro" }
          elseif ($os -eq "windows10") { $icon = "🪟"; $name = "Windows 10 Pro" }
          else { $icon = "🐧⚡"; $name = "Ubuntu Server" }
          Write-Host ""
          Write-Host "╔════════════════════════════════════════════════════╗"
          Write-Host "║  $icon $name - INSTANCE 3 $icon                ║"
          Write-Host "╚════════════════════════════════════════════════════╝"
          Write-Host "  🌍 Region : ${{ github.event.inputs.region }}"
          Write-Host "  👤 User   : ${{ github.event.inputs.username }}"
          Write-Host "  ⏱️ Hours  : ${{ github.event.inputs.duration }}"
          
      - name: RDP
        shell: pwsh
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          netsh advfirewall firewall add rule name="RDP-In" dir=in action=allow protocol=TCP localport=3389
          Set-Service -Name TermService -StartupType Automatic
          Start-Service -Name TermService
          
      - name: User
        shell: pwsh
        run: |
          $u = "${{ github.event.inputs.username }}"
          $p = "${{ github.event.inputs.password }}"
          $sec = ConvertTo-SecureString $p -AsPlainText -Force
          try { Remove-LocalUser -Name $u -ErrorAction SilentlyContinue } catch {}
          New-LocalUser -Name $u -Password $sec -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member $u
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member $u
          Enable-LocalUser -Name $u
          $p | Out-File -FilePath $env:TEMP\pass.txt
          
      - name: Wallpaper
        if: github.event.inputs.wallpaper_url != ''
        shell: pwsh
        run: |
          $url = "${{ github.event.inputs.wallpaper_url }}"
          $img = "$env:TEMP\wall.jpg"
          Invoke-WebRequest -Uri $url -OutFile $img
          Set-ItemProperty -Path "HKCU:\Control Panel\Desktop" -Name Wallpaper -Value $img
          Set-ItemProperty -Path "HKCU:\Control Panel\Desktop" -Name WallpaperStyle -Value 2
          
      - name: Screenshot
        shell: pwsh
        run: |
          Add-Type -AssemblyName System.Windows.Forms,System.Drawing
          $bmp = New-Object System.Drawing.Bitmap ([System.Windows.Forms.Screen]::PrimaryScreen.Bounds.Width), ([System.Windows.Forms.Screen]::PrimaryScreen.Bounds.Height)
          $g = [System.Drawing.Graphics]::FromImage($bmp)
          $g.CopyFromScreen(0, 0, 0, 0, $bmp.Size)
          $bmp.Save("$env:TEMP\screen.png")
          $g.Dispose(); $bmp.Dispose()
          
      - name: Tailscale
        shell: pwsh
        env:
          KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
        run: |
          $msi = "$env:TEMP\ts.msi"
          Invoke-WebRequest -Uri "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi" -OutFile $msi
          Start-Process msiexec.exe -ArgumentList "/i", "`"$msi`"", "/quiet", "/norestart" -Wait
          Remove-Item $msi -Force
          
      - name: Connect
        shell: pwsh
        env:
          KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=$env:KEY --hostname="vps-3"
          Start-Sleep -Seconds 5
          $ip = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
          echo "IP=$ip" >> $env:GITHUB_ENV
          
      - name: Benchmark
        shell: pwsh
        run: |
          $c = Get-Date
          $s = 0
          for ($i=0; $i -lt 1000000; $i++) { $s += $i }
          $cpu = [math]::Round(1000000 / (Get-Date).Subtract($c).TotalMilliseconds * 100, 2)
          $m = Get-Date
          $arr = New-Object System.Collections.ArrayList
          for ($i=0; $i -lt 500000; $i++) { $arr.Add($i) | Out-Null }
          $mem = [math]::Round(500000 / (Get-Date).Subtract($m).TotalMilliseconds * 100, 2)
          $d = Get-Date
          "X" * 10485760 | Out-File "$env:TEMP\disk.tmp"
          $disk = [math]::Round(10 / (Get-Date).Subtract($d).TotalSeconds, 2)
          Remove-Item "$env:TEMP\disk.tmp" -Force
          $bench = @()
          $bench += "CPU: $cpu ops/sec"
          $bench += "Memory: $mem ops/sec"
          $bench += "Disk: $disk MB/s"
          $bench | Out-File "$env:TEMP\bench.txt"
          
      - name: Monitor
        shell: pwsh
        run: |
          $cpu = (Get-WmiObject Win32_Processor).LoadPercentage
          $ram = Get-WmiObject Win32_OperatingSystem
          $ramU = [math]::Round(($ram.TotalVisibleMemorySize - $ram.FreePhysicalMemory) / 1MB, 2)
          $ramT = [math]::Round($ram.TotalVisibleMemorySize / 1MB, 2)
          $disk = Get-WmiObject Win32_LogicalDisk -Filter "DeviceID='C:'"
          $diskU = [math]::Round(($disk.Size - $disk.FreeSpace) / 1GB, 2)
          $diskT = [math]::Round($disk.Size / 1GB, 2)
          $up = (Get-Date) - (Get-CimInstance Win32_OperatingSystem).LastBootUpTime
          $res = @()
          $res += "CPU: $cpu%"
          $res += "RAM: $ramU/$ramT GB"
          $res += "DISK: $diskU/$diskT GB"
          $res += "UPTIME: $($up.Days)d $($up.Hours)h"
          $res | Out-File "$env:TEMP\res.txt"
          $hist = "$env:TEMP\history.csv"
          if (-not (Test-Path $hist)) { "Time,CPU,RAM_GB,DISK_GB" | Out-File $hist }
          "$(Get-Date -Format 'HH:mm:ss'),$cpu,$ramU,$diskU" | Out-File $hist -Append
          
      - name: Info
        shell: pwsh
        run: |
          $pass = Get-Content "$env:TEMP\pass.txt"
          $bench = Get-Content "$env:TEMP\bench.txt"
          $res = Get-Content "$env:TEMP\res.txt"
          $ip = "$env:IP"
          $user = "${{ github.event.inputs.username }}"
          $hours = "${{ github.event.inputs.duration }}"
          $start = (Get-Date).ToString('HH:mm:ss')
          $end = (Get-Date).AddHours($hours).ToString('HH:mm:ss')
          Write-Host ""
          Write-Host "╔════════════════════════════════════════════════════╗"
          Write-Host "║  🎉 VPS READY - INSTANCE 3 🎉                      ║"
          Write-Host "╠════════════════════════════════════════════════════╣"
          Write-Host "║  🌐 IP      : $ip"
          Write-Host "║  👤 User    : $user"
          Write-Host "║  🔑 Pass    : $pass"
          Write-Host "║  🔌 Port    : 3389"
          Write-Host "╠════════════════════════════════════════════════════╣"
          Write-Host "║  📊 BENCHMARK:"
          foreach ($l in $bench) { Write-Host "║     $l" }
          Write-Host "╠════════════════════════════════════════════════════╣"
          Write-Host "║  📊 RESOURCES:"
          foreach ($l in $res) { Write-Host "║     $l" }
          Write-Host "╠════════════════════════════════════════════════════╣"
          Write-Host "║  🕐 Started : $start"
          Write-Host "║  🕐 Expires : $end"
          Write-Host "╚════════════════════════════════════════════════════╝"
          Write-Host ""
          $info = @()
          $info += "Host: $ip"
          $info += "Port: 3389"
          $info += "User: $user"
          $info += "Pass: $pass"
          $info | Out-File vps-info.txt -Encoding ascii
          
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: vps-data-3
          path: |
            vps-info.txt
            ${{ env.TEMP }}\screen.png
            ${{ env.TEMP }}\bench.txt
            ${{ env.TEMP }}\history.csv
          retention-days: 7
          
      - name: Keep
        shell: pwsh
        run: |
          $h = ${{ github.event.inputs.duration }}
          Start-Sleep -Seconds ($h * 3600)
