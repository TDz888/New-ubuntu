name: 🚀 PREMIUM VPS GENERATOR

on:
  workflow_dispatch:
    inputs:
      os_type:
        description: 🖥️ Operating System
        required: true
        type: choice
        default: windows-latest
        options:
          - windows-latest
          - ubuntu-latest
      duration:
        description: ⏱️ Duration
        required: true
        type: choice
        default: 3h
        options:
          - 30m
          - 1h
          - 2h
          - 3h
          - 4h
          - max

jobs:
  generate-vps:
    runs-on: ${{ github.event.inputs.os_type }}
    timeout-minutes: 360
    
    steps:
      - name: SYSTEM INIT
        shell: pwsh
        run: |
          Write-Host ""
          Write-Host "========================================" -ForegroundColor Cyan
          Write-Host "   PREMIUM VPS GENERATOR - STARTING" -ForegroundColor Cyan
          Write-Host "========================================" -ForegroundColor Cyan
          Write-Host ""
          Write-Host "System ready!" -ForegroundColor Green

      - name: CONFIGURE WINDOWS RDP
        if: contains(github.event.inputs.os_type, 'windows')
        shell: pwsh
        run: |
          Write-Host "Configuring Remote Desktop..." -ForegroundColor Yellow
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          netsh advfirewall firewall add rule name="RDP-VPS" dir=in action=allow protocol=TCP localport=3389
          Set-Service -Name TermService -StartupType Automatic
          Start-Service -Name TermService
          Write-Host "RDP configured!" -ForegroundColor Green

      - name: CONFIGURE LINUX SSH
        if: contains(github.event.inputs.os_type, 'ubuntu')
        shell: bash
        run: |
          echo "Configuring SSH..."
          sudo sed -i 's/PasswordAuthentication no/PasswordAuthentication yes/' /etc/ssh/sshd_config
          sudo systemctl restart sshd
          sudo ufw allow 22/tcp
          echo "SSH configured!"

      - name: CREATE USER ACCOUNT
        shell: pwsh
        run: |
          Write-Host "Creating user account..." -ForegroundColor Yellow
          
          $upper = 'ABCDEFGHJKLMNPQRSTUVWXYZ'
          $lower = 'abcdefghijkmnpqrstuvwxyz'
          $numbers = '23456789'
          $all = $upper + $lower + $numbers
          
          $pass = $upper[(Get-Random -Max $upper.Length)]
          $pass += $lower[(Get-Random -Max $lower.Length)]
          $pass += $numbers[(Get-Random -Max $numbers.Length)]
          
          for ($i = 1; $i -le 5; $i++) {
              $pass += $all[(Get-Random -Max $all.Length)]
          }
          
          $chars = $pass.ToCharArray()
          $pass = -join ($chars | Sort-Object { Get-Random })
          
          $username = "PREMIUM-VPS"
          $securePass = ConvertTo-SecureString $pass -AsPlainText -Force
          
          if ($env:OS_TYPE -eq "windows-latest") {
              Remove-LocalUser -Name $username -ErrorAction SilentlyContinue
              New-LocalUser -Name $username -Password $securePass -AccountNeverExpires
              Add-LocalGroupMember -Group "Administrators" -Member $username
              Add-LocalGroupMember -Group "Remote Desktop Users" -Member $username
              Enable-LocalUser -Name $username
          } else {
              sudo useradd -m -s /bin/bash $username
              echo "$username:$pass" | sudo chpasswd
              sudo usermod -aG sudo $username
          }
          
          echo "VPS_USER=$username" >> $env:GITHUB_ENV
          echo "VPS_PASS=$pass" >> $env:GITHUB_ENV
          $pass | Out-File -FilePath $env:TEMP\vps_pass.txt
          
          Write-Host "Username: $username" -ForegroundColor Green
          Write-Host "Password: $pass" -ForegroundColor Green

      - name: INSTALL TAILSCALE
        env:
          TAILSCALE_AUTH_KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
        shell: pwsh
        run: |
          Write-Host "Installing Tailscale..." -ForegroundColor Yellow
          
          if ($env:OS_TYPE -eq "windows-latest") {
              $msiPath = "$env:TEMP\tailscale.msi"
              Invoke-WebRequest -Uri "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi" -OutFile $msiPath
              Start-Process msiexec.exe -ArgumentList "/i `"$msiPath`" /quiet /norestart" -Wait
              Remove-Item $msiPath -Force
              Start-Sleep -Seconds 5
              & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=$env:TAILSCALE_AUTH_KEY --hostname="premium-vps-$env:GITHUB_RUN_ID"
          } else {
              curl -fsSL https://tailscale.com/install.sh | sudo sh
              sudo tailscale up --authkey=$env:TAILSCALE_AUTH_KEY --hostname="premium-vps-$env:GITHUB_RUN_ID"
          }
          
          Write-Host "Tailscale installed!" -ForegroundColor Green

      - name: GET TAILSCALE IP
        shell: pwsh
        run: |
          Write-Host "Getting Tailscale IP..." -ForegroundColor Yellow
          
          $ip = ""
          for ($i = 0; $i -lt 15; $i++) {
              try {
                  if ($env:OS_TYPE -eq "windows-latest") {
                      $ip = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4 2>&1
                  } else {
                      $ip = bash -c "tailscale ip -4" 2>&1
                  }
                  if ($ip -match '^\d+\.\d+\.\d+\.\d+$') {
                      echo "TAILSCALE_IP=$ip" >> $env:GITHUB_ENV
                      Write-Host "IP: $ip" -ForegroundColor Green
                      break
                  }
              } catch {}
              Start-Sleep -Seconds 2
          }
          
          if ($ip -eq "") {
              echo "TAILSCALE_IP=localhost" >> $env:GITHUB_ENV
              Write-Host "Using localhost" -ForegroundColor Yellow
          }

      - name: SYSTEM INFO
        shell: pwsh
        run: |
          Write-Host "System Information..." -ForegroundColor Yellow
          
          if ($env:OS_TYPE -eq "windows-latest") {
              $cpu = (Get-CimInstance Win32_Processor).NumberOfCores
              $ram = [math]::Round((Get-CimInstance Win32_ComputerSystem).TotalPhysicalMemory / 1GB, 1)
              echo "SYS_CPU=$cpu cores" >> $env:GITHUB_ENV
              echo "SYS_RAM=${ram}GB" >> $env:GITHUB_ENV
              Write-Host "CPU: $cpu cores" -ForegroundColor White
              Write-Host "RAM: ${ram}GB" -ForegroundColor White
          } else {
              $cpu = bash -c "nproc"
              $ram = bash -c "free -h | awk '/^Mem:/ {print \$2}'"
              echo "SYS_CPU=$cpu cores" >> $env:GITHUB_ENV
              echo "SYS_RAM=$ram" >> $env:GITHUB_ENV
              Write-Host "CPU: $cpu cores" -ForegroundColor White
              Write-Host "RAM: $ram" -ForegroundColor White
          }

      - name: SEND TELEGRAM NOTIFICATION
        if: success()
        env:
          BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        shell: pwsh
        run: |
          $bot = $env:BOT_TOKEN
          $chat = $env:CHAT_ID
          
          if ($bot -eq "" -or $chat -eq "") {
              Write-Host "No Telegram credentials - skipping" -ForegroundColor Yellow
              exit 0
          }
          
          $pass = Get-Content $env:TEMP\vps_pass.txt
          $duration = "${{ github.event.inputs.duration }}"
          
          $min = 0
          if ($duration -eq "30m") { $min = 30 }
          if ($duration -eq "1h") { $min = 60 }
          if ($duration -eq "2h") { $min = 120 }
          if ($duration -eq "3h") { $min = 180 }
          if ($duration -eq "4h") { $min = 240 }
          if ($duration -eq "max") { $min = 340 }
          
          $now = Get-Date
          $end = $now.AddMinutes($min)
          
          $msg = "PREMIUM VPS READY"
          $msg = $msg + "%0A━━━━━━━━━━━━━━━━━━━━━"
          $msg = $msg + "%0AOS: ${{ github.event.inputs.os_type }}"
          $msg = $msg + "%0AUser: $env:VPS_USER"
          $msg = $msg + "%0APass: $pass"
          $msg = $msg + "%0AIP: $env:TAILSCALE_IP"
          $msg = $msg + "%0ACPU: $env:SYS_CPU"
          $msg = $msg + "%0ARAM: $env:SYS_RAM"
          $msg = $msg + "%0ADuration: $duration"
          $msg = $msg + "%0AStart: $($now.ToString('HH:mm:ss'))"
          $msg = $msg + "%0AEnd: $($end.ToString('HH:mm:ss'))"
          
          if ($env:OS_TYPE -eq "windows-latest") {
              $msg = $msg + "%0AConnect: mstsc /v:$env:TAILSCALE_IP"
          } else {
              $msg = $msg + "%0AConnect: ssh $env:VPS_USER@$env:TAILSCALE_IP"
          }
          
          $url = "https://api.telegram.org/bot$bot/sendMessage"
          $body = "chat_id=$chat&text=$msg&parse_mode=Markdown"
          
          Invoke-RestMethod -Uri $url -Method Post -Body $body
          Write-Host "Telegram sent!" -ForegroundColor Green

      - name: SHOW CONNECTION INFO
        shell: pwsh
        run: |
          $pass = Get-Content $env:TEMP\vps_pass.txt
          $duration = "${{ github.event.inputs.duration }}"
          
          $min = 0
          if ($duration -eq "30m") { $min = 30 }
          if ($duration -eq "1h") { $min = 60 }
          if ($duration -eq "2h") { $min = 120 }
          if ($duration -eq "3h") { $min = 180 }
          if ($duration -eq "4h") { $min = 240 }
          if ($duration -eq "max") { $min = 340 }
          
          $now = Get-Date
          $end = $now.AddMinutes($min)
          echo "TOTAL_MIN=$min" >> $env:GITHUB_ENV
          
          Write-Host ""
          Write-Host "=================================================" -ForegroundColor Cyan
          Write-Host "         PREMIUM VPS READY - CONNECT NOW" -ForegroundColor Cyan
          Write-Host "=================================================" -ForegroundColor Cyan
          Write-Host "IP Address   : $env:TAILSCALE_IP" -ForegroundColor White
          Write-Host "Username     : $env:VPS_USER" -ForegroundColor White
          Write-Host "Password     : $pass" -ForegroundColor White
          Write-Host "OS           : ${{ github.event.inputs.os_type }}" -ForegroundColor White
          Write-Host "CPU          : $env:SYS_CPU" -ForegroundColor White
          Write-Host "RAM          : $env:SYS_RAM" -ForegroundColor White
          Write-Host "-------------------------------------------------" -ForegroundColor Cyan
          Write-Host "Duration     : $duration" -ForegroundColor White
          Write-Host "Start        : $($now.ToString('HH:mm:ss'))" -ForegroundColor White
          Write-Host "End          : $($end.ToString('HH:mm:ss'))" -ForegroundColor White
          Write-Host "-------------------------------------------------" -ForegroundColor Cyan
          
          if ($env:OS_TYPE -eq "windows-latest") {
              Write-Host "RDP Connect  : mstsc /v:$env:TAILSCALE_IP" -ForegroundColor Yellow
          } else {
              Write-Host "SSH Connect  : ssh $env:VPS_USER@$env:TAILSCALE_IP" -ForegroundColor Yellow
          }
          
          Write-Host "=================================================" -ForegroundColor Cyan
          Write-Host ""

      - name: KEEP ALIVE
        shell: pwsh
        run: |
          $min = [int]$env:TOTAL_MIN
          $end = (Get-Date).AddMinutes($min)
          
          Write-Host "Keeping VPS alive..." -ForegroundColor Yellow
          Write-Host "IP: $env:TAILSCALE_IP" -ForegroundColor Cyan
          Write-Host "End: $($end.ToString('HH:mm:ss'))" -ForegroundColor Green
          Write-Host ""
          
          do {
              $now = Get-Date
              $remain = $end - $now
              
              if ($remain.TotalSeconds -le 0) { break }
              
              $h = [math]::Floor($remain.TotalHours)
              $m = [math]::Floor($remain.TotalMinutes % 60)
              $s = [math]::Floor($remain.TotalSeconds % 60)
              
              Write-Host "`r[$($now.ToString('HH:mm:ss'))] Remaining: ${h}h ${m}m ${s}s" -NoNewline -ForegroundColor Yellow
              
              Start-Sleep -Seconds 1
              
          } while ($remain.TotalSeconds -gt 0)
          
          Write-Host "`n`nTIME EXPIRED - VPS SHUTTING DOWN!" -ForegroundColor Red

      - name: CLEANUP
        if: always()
        shell: pwsh
        run: |
          Write-Host "Cleaning up..." -ForegroundColor Yellow
          
          Remove-Item "$env:TEMP\vps_pass.txt" -ErrorAction SilentlyContinue
          
          if ($env:OS_TYPE -eq "windows-latest") {
              Disable-LocalUser -Name $env:VPS_USER -ErrorAction SilentlyContinue
              netsh advfirewall firewall delete rule name="RDP-VPS" -ErrorAction SilentlyContinue
              & "$env:ProgramFiles\Tailscale\tailscale.exe" down -ErrorAction SilentlyContinue
          } else {
              sudo tailscale down -ErrorAction SilentlyContinue
          }
          
          Write-Host "Cleanup complete! Thank you!" -ForegroundColor Green
